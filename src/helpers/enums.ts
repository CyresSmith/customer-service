export enum ServiceOpenModal {
    ADD = 1,
    CREATE = 2,
    CREATE_CATEGORY = 3,
    EDIT_SERVICE = 4,
    DELETE_SERVICE = 5,
    ADD_TO_EMPLOYEE,
}

export enum ServiceTypeEnum {
    INDIVIDUAL = 'individual',
    GROUP = 'group',
}
